I will replace all instances of the blue color scheme with green across the application's frontend files.

1.  **Search & Replace**: I will execute a command to find all TypeScript/React files (`.ts`, `.tsx`) in `resources/js` and replace every occurrence of the Tailwind utility class prefix `blue-` with `green-`.
    *   This covers backgrounds (`bg-blue-600` → `bg-green-600`), text (`text-blue-600` → `text-green-600`), borders, rings, and other utilities.
    *   Target files include pages (Dashboard, Welcome, Auth), components (Logo, Footer), and layouts.

2.  **Verify**: I will check key files like `dashboard.tsx` and `app-logo.tsx` to ensure the replacement was successful and no invalid class names were generated.

3.  **Outcome**: The website's primary brand color (buttons, links, active states, badges, logos) will change from Blue to Green. The "White" part of your request is already the default background, which remains unchanged.